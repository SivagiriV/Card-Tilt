import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
    root: {
      display: 'flex',
      alignItems: 'center',
      height:"32px",
      border: "none",
      marginTop:20,
    },
    input: {
      flex: 1,
      color: "#41477A"
    },
    iconButton: {
      padding: 10,
    }
  });

  export default useStyles
